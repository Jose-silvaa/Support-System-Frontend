/**
 * Storybook: adicione @storybook/react e descomente o exemplo abaixo.
 *
 * import type { Meta, StoryObj } from "@storybook/react"
 * import { AppCard } from "./index"
 *
 * const meta: Meta<typeof AppCard> = {
 *   component: AppCard,
 *   title: "Components/AppCard",
 * }
 * export default meta
 *
 * type Story = StoryObj<typeof AppCard>
 *
 * export const Default: Story = { args: { children: "Conteúdo do card." } }
 * export const WithTitle: Story = { args: { title: "Título", children: "Conteúdo." } }
 */

export {}
